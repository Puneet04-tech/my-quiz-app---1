# Example environment variables for deploying My Quiz App
# For Render set these in the Service > Environment panel (do NOT commit secret values to git)

# Optional: port
PORT=3000
NODE_ENV=production

# Optional admin credentials to protect scores page
ADMIN_USER=faculty
ADMIN_PASS=change_this_to_a_strong_password

# Firebase service account JSON (recommended)
# Recommended: paste the entire JSON string as the value of FIREBASE_SERVICE_ACCOUNT.
# If your host doesn't accept multiline values, base64-encode the JSON file and set the base64 string.
# Example (local):
# $json = Get-Content serviceAccount.json -Raw
# $env:FIREBASE_SERVICE_ACCOUNT = $json
# OR base64 encode:
# $base64 = [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($json))
# Set FIREBASE_SERVICE_ACCOUNT to that $base64 string.
FIREBASE_SERVICE_ACCOUNT="{...paste service account JSON here...}"

# Alternative durable storage (S3) - choose either Firestore or S3, not both
# S3_BUCKET=my-quiz-app-scores-2026
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=YOUR_KEY_ID
# AWS_SECRET_ACCESS_KEY=YOUR_SECRET

# Optional Postgres (not required if using Firestore or S3)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Notes:
# - Use either Firestore (recommended free-friendly) or S3/DB for persistence.
# - Keep service account JSON private and do not commit it to source control.
